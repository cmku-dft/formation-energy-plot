{
  "name": "formation-energy-plot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check scripts/build.cjs",
    "test": "node --test tests/*.test.cjs",
    "build:pages": "node scripts/build.cjs"
  }
}
